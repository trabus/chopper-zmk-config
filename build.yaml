# .github/workflows/build.yml
name: Build

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    strategy:
      matrix:
        include:
          - board: nice_nano_v2
            shield: chopper_unibody
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
    with:
      build_matrix_path: "" # Disables external build.yaml lookup to prevent conflicts
